export class EmployeeDTO {
  constructor(public firstName: string | undefined, public lastName: string | undefined, public street: string | undefined, public postcode: string | undefined, public city: string | undefined, public phone: string | undefined) {
  }
}
